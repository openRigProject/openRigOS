# RPi Zero 2 W specific packages
# (WiFi/hostapd/dnsmasq are in packages/base.list)

# Bluetooth
bluez
bluez-tools

# Serial / GPIO (ham radio interfacing)
python3-serial
python3-rpi.gpio
raspi-gpio

# System tools
raspi-config
